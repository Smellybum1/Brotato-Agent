# WP1 v69 gate handoff

Updated: 2026-07-20 20:50 Australia/Brisbane

## Read first

1. `reports/live_monitor.json`
2. `reports/WP1_ACTIVE_GATE.md`
3. Run `powershell -ExecutionPolicy Bypass -File scripts/check_gate_status.ps1`

Avoid raw logs, full event streams, historical deploy records, Fable's review
prompt, and the WP2 packet unless an actionable signal requires drilldown.

## Current state

- Active version: v69 (`0.1.69-gun-wp1`).
- Gate: fresh 20-run evaluation requiring at least 18 wins.
- Handoff snapshot: 0W/1L; run 1 lost cleanly at wave 16 and run 2 was active
  with fresh telemetry. Re-read the live files immediately because the gate is
  running and this count will change.
- Both exact v69 scheduled tasks were Running with restart count 999, restart
  interval `PT1M`, and `StopOnIdleEnd=false`; their scoped process trees and
  Brotato were running. No Brotato APPCRASH was found after the v69 gate start.
- Fable implemented the v69 below-target offense floor; Codex independently
  verified 47/47 tests, deployed it, and started this gate before this rollover.
- The broad any-gun pool remains intentional. The recently reviewed Danger 5
  resources favor compatible rapid-fire families, but do not change v69 solely
  for that observation while the gate is active.
- Repository: Git on branch `master`, no commits or upstream. The entire project
  is untracked, so Git cannot distinguish ownership or provide a useful baseline.
  Do not commit unless the user asks.

## Optimize-codex changes in the source task

- Added `scripts/check_gate_status.ps1`, a read-only compact snapshot for HUD,
  supervisor signals, exact task settings, scoped watchdog processes, Brotato,
  newest summary, and APPCRASH events.
- Updated `reports/WP1_ACTIVE_GATE.md` and `README.md` to use that helper after
  reading `live_monitor.json` first.
- Validation: PowerShell parser reported zero errors; helper output parsed as
  JSON; tasks/processes/Brotato were healthy; APPCRASH count was zero;
  `git diff --check` passed (Git has no tracked baseline).
- No gameplay code, dependencies, deployment, scheduled task, or game process
  was changed by optimization.

## Guardrails

- Follow `reports/WP1_ACTIVE_GATE.md`; its active version overrides stale text.
- If healthy, report only material progress and continue monitoring.
- Stop only on the runbook's actionable conditions. Three total losses or
  `GATE IMPOSSIBLE` triggers the focused-repair workflow.
- A required stop may affect only the exact active-version BrotatoAgent tasks,
  positively identified descendants under this checkout, and Brotato itself.
  Never kill generic consoles or unrecognized Python/PowerShell/cmd processes.
  Slay the Spire 2 and Claude processes/windows are out of scope.
- Exactly one focused version-bumped repair per trigger; full tests, deploy
  verification, then a fresh gate. Do not start WP2 until WP1 passes and its
  closeout checklist is complete. Do not spawn subagents for this gate.

## Next task

Own and monitor the active v69 gate. Read the live monitor first, run the compact
status helper, apply the runbook's informational-warning exceptions, and keep
the gate running unless an actionable condition occurs. If a trigger occurs,
preserve evidence and follow the runbook's scoped stop and one-repair process.

## Cleanup manifest

- `workspace_type:` Git repository with no commits
- `root_path:` `C:\Codex\Brotato Agent`
- `stale_paths_fixed:` none; active absolute paths were valid
- `stale_paths_reported_only:` historical/generated absolute paths only
- `files_shortened_or_archived:` none
- `workflow_helpers_added_or_updated:` `scripts/check_gate_status.ps1`, active
  gate runbook, README
- `startup_context_before:` active monitor + runbook, about 2.7k tokens at the
  final snapshot, plus repeated raw command/log output
- `startup_context_after:` same authoritative files, about 2.7k tokens; compact
  helper adds about 1.2k tokens only when invoked and replaces larger drilldowns
- `validation_gate:` PowerShell parse, live JSON execution, infrastructure
  assertions, crash query, compact Git status, `git diff --check`
- `left_alone:` gameplay code, user/Fable work, raw/generated reports, old task
  definitions, dependencies, deployment, running gate, WP2
